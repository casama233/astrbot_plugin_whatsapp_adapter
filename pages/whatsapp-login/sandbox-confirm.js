(() => {
  "use strict";
  // Updater v2 and logout confirmations are implemented as explicit two-step
  // state machines in app.js.  Keep this legacy asset as a harmless no-op for
  // already cached Plugin Page HTML that may still request the old filename.
})();
